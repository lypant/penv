"""This is example code project code"""

def add(summand_a: int, summand_b: int) -> int:
    """Adds to integers and returns its sum"""
    return summand_a + summand_b
    # x = 'this is very very very very long script that has no obvious reason to exist'
